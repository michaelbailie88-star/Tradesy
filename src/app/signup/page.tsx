import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account — Vettd",
  description: "Create your Vettd account to post jobs, find contractors, or grow your trade business.",
};

export { default } from "./SignupForm";

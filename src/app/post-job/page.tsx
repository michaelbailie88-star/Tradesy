import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Post a Job — Vettd",
  description: "Post your home improvement project and receive free bids from verified local contractors.",
};

export { default } from "./PostJobForm";